import React from "react";

export default () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        marginBottom: "2em",
        paddingTop: "2em",
        borderTop: "2px solid var(--gray)",
      }}
    >
      <a href="https://iguacel.github.io/ascensorista-espacial/">
        <h1>Prod</h1>
      </a>
    </div>
  );
}
